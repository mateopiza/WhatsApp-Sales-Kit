Circular icon-only button (search, cart, wishlist, close) for headers and card overlays.

```jsx
<IconButton icon={<Icon name="cart" />} ariaLabel="Carrito" variant="outline" />
```
Variants: `ghost`, `outline`, `filled`.
