Uppercase, tracked-out call-to-action button used across storefront and marketing surfaces.

```jsx
<Button variant="primary" size="md">Añadir al carrito</Button>
```

Variants: `primary` (ink fill), `secondary` (ink outline), `gold` (accent fill), `ghost` (text-only, taupe). Sizes: `sm`, `md`, `lg`.
